import Timer from "./Components/shared/Timer.jsx";
function App() {
  return (
    <div
      style={{
        width: "70%",
        margin: "auto",
        display: "flex",
        alignItems: "center",
        flexDirection: "column",
      }}
    >
      <Timer></Timer>
    </div>
  );
}

export default App;
